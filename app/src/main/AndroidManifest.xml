<?xml version="1.0" encoding="utf-8"?>
<!-- This is my main manifest file where I set up all the permissions and register all the screens -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <!-- Basic permissions the app needs (internet & location) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camera & gallery access for uploading the IC/passport image -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- These allow the image picker to work properly -->
    <queries>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
            <category android:name="android.intent.category.DEFAULT" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PICK" />
            <data android:mimeType="image/*" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="34">

        <!-- My Google Maps API key so the map works -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAuyObDq4g1RquSPODGgWO3DlIf9l4nN6g" />

        <!-- File provider (needed so the app can open camera/gallery safely) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- This is the first screen that opens when the app launches -->
        <activity
            android:name="com.example.smartrentbn.PreLoginActivity"
            android:exported="true"
            android:theme="@style/ThemeWithoutActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login and signup pages -->
        <activity
            android:name="com.example.smartrentbn.LoginActivity"
            android:exported="true"
            android:theme="@style/ThemeWithoutActionBar" />

        <activity
            android:name="com.example.smartrentbn.SignupActivity"
            android:exported="true"
            android:theme="@style/ThemeWithoutActionBar" />

        <!-- Main house listing screen -->
        <activity
            android:name="com.example.smartrentbn.HouseListActivity"
            android:exported="false"
            android:theme="@style/ThemeWithActionBar" />

        <!-- House details page -->
        <activity
            android:name="com.example.smartrentbn.HouseDetailActivity"
            android:exported="false"
            android:theme="@style/ThemeWithActionBar" />

        <!-- Tenant application form -->
        <activity
            android:name="com.example.smartrentbn.RentalFormActivity"
            android:exported="false"
            android:theme="@style/ThemeWithActionBar" />

        <!-- Landlord's page to see applications -->
        <activity
            android:name="com.example.smartrentbn.TenantApplicationsActivity"
            android:exported="false"
            android:theme="@style/ThemeWithActionBar" />

        <!-- Full map page -->
        <activity
            android:name="com.example.smartrentbn.MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps"
            android:theme="@style/ThemeWithActionBar" />

        <!-- Small dialog map when choosing location -->
        <activity
            android:name="com.example.smartrentbn.MapDialogActivity"
            android:exported="false"
            android:theme="@style/ThemeWithActionBar"
            android:label="SmartRent BN" />

        <!-- Add new house screen -->
        <activity
            android:name="com.example.smartrentbn.AddHouseActivity2"
            android:exported="false"
            android:theme="@style/ThemeWithActionBar" />

        <!-- Landlord dashboard screen -->
        <activity
            android:name="com.example.smartrentbn.LandlordDashboardActivity"
            android:exported="false"
            android:theme="@style/ThemeWithActionBar" />

        <!-- Base activity for shared navbar/header -->
        <activity
            android:name="com.example.smartrentbn.BaseActivity"
            android:exported="false"
            android:theme="@style/ThemeWithActionBar" />

        <!-- Contact us page -->
        <activity
            android:name="com.example.smartrentbn.ContactUsActivity"
            android:exported="false"
            android:theme="@style/ThemeWithActionBar" />

        <!-- About us page -->
        <activity
            android:name="com.example.smartrentbn.AboutUsActivity"
            android:exported="false"
            android:theme="@style/ThemeWithActionBar" />

    </application>

</manifest>
